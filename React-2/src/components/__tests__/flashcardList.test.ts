import { render, screen } from '@testing-library/react';
import { FlashcardList } from '../../features/flashcardtranslator/FlashcardList';

test('renders FlashcardList without crashing', () => {

});
